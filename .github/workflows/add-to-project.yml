name: Adiciona issues e PRs ao Kanban

on:
  issues:
    types: [opened, reopened]
  pull_request:
    types: [opened, reopened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/users/lucasgerez/projects/1
          github-token: ${{ secrets.PROJECT_TOKEN }}
